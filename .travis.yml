language: node_js
sudo: required
services:
  - redis-server
before_script:
  # - nvm install
script:
  # - npm install
  - npm test
branches:
  only:
    - master
cache:
  directories:
    - node_modules
